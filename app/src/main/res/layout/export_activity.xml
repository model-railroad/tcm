<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

    <Button
            android:id="@+id/btn_share"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:layout_marginStart="16dp"
            android:drawableLeft="@drawable/ic_share"
            android:text="@string/export__share_title"
        />

    <Button
            android:id="@+id/btn_load"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignBaseline="@id/btn_share"
            android:layout_alignEnd="@+id/edit_text"
            android:drawableLeft="@drawable/ic_reload"
            android:text="@string/export__load_title"
            />

    <EditText
            android:id="@+id/edit_text"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_margin="16dp"
            android:layout_below="@id/btn_share"
            android:hint="@string/export__edit_hint"
            android:inputType="textMultiLine"
            android:autofillHints=""
            android:elevation="1px"
            />
</RelativeLayout>
