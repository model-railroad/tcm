<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TrackCamMonitor" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <!-- App Theme -->
    <style name="Theme.TrackCamMonitorApp" parent="Base.Theme.TrackCamMonitor" />

    <!-- Main Activity Theme -->
    <style name="Theme.MainActivity" parent="Theme.TrackCamMonitorApp">
        <!-- API 21: allows nav bar to draw above a stable layout, without resizing it. -->
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowTranslucentNavigation">true</item>

    </style>
</resources>
